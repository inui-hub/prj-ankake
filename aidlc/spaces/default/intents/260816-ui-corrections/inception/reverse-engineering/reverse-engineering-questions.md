# Reverse Engineering Questions

## Code Knowledge Base

The existing code knowledge base for `prj-ankake` is **UNVERIFIED**: it was built for `resonance-feature` and has no recorded fingerprint, so its freshness cannot be confirmed. Its deep coverage is limited to battle-related packages, selected tests, and two battle specifications; the current UI-corrections work also covers menu, deck, and battle UI.

### Q1. How should the code knowledge base be refreshed?

A. Full rescan — rebuild coverage for the whole repository
B. Focused scan — inspect the UI-corrections area; the replacement knowledge base will cover only that area
X. Other (please specify)

[Answer]: Full rescan

## Consolidated Summary Confirmation

- Refresh `prj-ankake` with a full repository scan.

Does this all look correct before I generate the artifact?

- Looks correct
- Request changes

[Answer]: Looks correct
